{
  "name": "staticblog",
  "version": "1.0.0",
  "description": "바닐라 JS를 이용하여 정적 블로그 만들기",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "static-server pages",
    "build": "(rmdir /s /q dist || echo.) && node ./scripts/build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "static-server dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "front-matter": "^4.0.2",
    "mustache": "^4.2.0",
    "showdown": "^2.1.0",
    "static-server": "^2.2.1"
  }
}
